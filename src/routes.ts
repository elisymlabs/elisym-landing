/**
 * Route definitions for the landing site.
 * Used by both client-side router and SSG script.
 * Add new pages here - SSG will automatically pre-render them.
 */
export const routes = [
  '/',
  // Add future pages here, e.g.:
  // "/about",
  // "/docs",
  // "/pricing",
];
